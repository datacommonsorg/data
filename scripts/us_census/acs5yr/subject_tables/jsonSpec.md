## Documentating on making a JSON Specification for a ACS Subject Table

### What is the JSON Spec? Why do we need it?

### Structure of the JSON Spec

#### `populationType`

#### `measurement`

#### `pvs`

#### `universePVs`

#### `ignoreColumns`

#### `denominators`


> Future additions, `units` and `scalingFactor`